<h2>Neue Strafe vergeben</h2>
<% if (error) { %>
  <div class="error"><%= error %></div>
<% } %>
<form method="POST" class="form-card">
  <label for="user_id">Nutzer:</label>
  <select name="user_id" id="user_id" required>
    <% users.forEach(u => { %>
      <option value="<%= u.id %>"><%= u.username %></option>
    <% }) %>
  </select>
  
  <label for="type">Grund:</label>
  <input type="text" name="type" id="type" required>

  <label for="event">Veranstaltung:</label>
  <input type="text" name="event" id="event" required>

  <label for="amount">Betrag (€):</label>
  <input type="number" name="amount" id="amount" min="0" step="0.01" required placeholder="z.B. 3.50">

  <label for="date">Datum:</label>
  <input type="date" name="date" id="date" required value="<%= new Date().toISOString().slice(0,10) %>">

  <button type="submit" class="btn">➕ Eintragen</button>
  <a href="/penalties" class="btn btn-cancel">Abbrechen</a>
</form>
