<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title : 'Spießbuch' %></title>
  <meta name="theme-color" content="#b0b99b">
  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/logo.png">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inter:400,500,600&display=swap" />
  <style>
    /* --- Root CSS & Basis Styles --- */
    :root {
      --green-dark: #606e56;
      --nav-bg: rgba(255 255 255 / 0.65);
      --radius: 1.85rem;
      --font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      --blur: blur(12px);
      --accent: #a0bb8c;
    }
    html, body {
      margin: 0;
      padding: 0;
      overflow-x: hidden;
      width: 100vw;
      box-sizing: border-box;
      font-family: var(--font);
      font-size: 17.5px;
      background: #f8fafb;
      color: #222;
      scroll-behavior: smooth;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    *, *::before, *::after {
      box-sizing: inherit;
    }
    body {
      min-height: 100dvh;
      display: flex;
      flex-direction: column;
    }
    /* --- Navigation --- */
    .main-nav {
      position: sticky;
      top: 0;
      z-index: 99;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.7rem 2.4rem 0.7rem 0;
      background: var(--nav-bg);
      border-radius: 0 0 var(--radius) var(--radius);
      min-height: 64px;
      backdrop-filter: var(--blur);
      -webkit-backdrop-filter: var(--blur);
      user-select: none;
    }
    .main-nav .logo {
      display: flex;
      align-items: center;
      gap: 1.2em;
      font-size: 2.4rem;
      font-weight: 900;
      color: var(--green-dark);
      letter-spacing: -0.025em;
      text-decoration: none;
      user-select: text;
      line-height: 1;
      padding-left: 1rem;
    }
    .main-nav .logo img {
      height: 56px;
      width: 56px;
      object-fit: contain;
      border-radius: 0;
      box-shadow: none;
      background: none;
      filter: none;
      user-select: none;
      pointer-events: none;
      transition: none;
    }
    .nav-menu {
      display: flex;
      gap: 0.38em;
      align-items: center;
      list-style: none;
      margin: 0;
      padding: 0;
      user-select: none;
    }
    .nav-menu li {
      position: relative;
    }
    .nav-menu a,
    .btn-logout {
      display: flex;
      align-items: center;
      gap: 0.31em;
      background: none;
      border: none;
      font-size: 1.1rem;
      color: var(--green-dark);
      text-decoration: none;
      padding: 0.64em 1.27em;
      border-radius: 1.1rem;
      transition:
        background 0.22s ease,
        color 0.18s ease,
        box-shadow 0.3s ease,
        filter 0.18s ease;
      font-weight: 500;
      cursor: pointer;
      outline: none;
      position: relative;
      overflow: hidden;
      will-change: background, box-shadow, filter;
      user-select: text;
    }
    .nav-menu a:hover,
    .btn-logout:hover {
      background: #dce1d5;
      color: var(--green-dark);
      box-shadow: none;
      filter: none;
    }
    .nav-menu a:focus-visible,
    .btn-logout:focus-visible {
      outline-offset: 4px;
      outline: 3px solid #a0bb8ccc;
      background: rgba(170 188 150 / 0.15);
    }
    .btn-logout svg {
      vertical-align: middle;
      margin-left: 3px;
      stroke-width: 2.3;
      stroke-linejoin: round;
    }
    .nav-menu a:active,
    .btn:active {
      box-shadow: none;
      filter: none;
      transition: none;
    }
    .hamburger-btn {
      display: none;
      flex-direction: column;
      gap: 4.3px;
      background: none;
      border: none;
      cursor: pointer;
      padding: 7px 7px;
      border-radius: 13px;
      transition: background 0.23s ease;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
      position: relative;
      z-index: 110;
      width: 40px;
      height: 32px;
    }
    .hamburger-btn span {
      display: block;
      width: 30px;
      height: 4px;
      background: var(--green-dark);
      border-radius: 4px;
      transition: 0.3s ease;
      transform-origin: 3px 3px;
      will-change: transform;
      position: relative;
    }
    .hamburger-btn.open span:nth-child(1) {
      transform: rotate(45deg) translate(6px, 7px);
    }
    .hamburger-btn.open span:nth-child(2) {
      opacity: 0;
      transform: translateX(-34px);
    }
    .hamburger-btn.open span:nth-child(3) {
      transform: rotate(-45deg) translate(7px, -8px);
    }
    .hamburger-btn:focus-visible {
      outline-offset: 4px;
      outline: 3px solid #a0bb8ccc;
    }
    .hamburger-btn:active {
      background: #dce1d5;
    }
    /* --- Main Content Container --- */
    main.container {
      flex: 1;
      width: 100%;
      max-width: 680px;
      box-sizing: border-box;
      margin: 2.6rem auto 2.8rem auto;
      background: rgba(255 255 255 / 0.55);
      border-radius: var(--radius);
      box-shadow: 0 12px 40px rgba(122 142 111 / 0.14), 0 4px 16px rgba(181 198 164 / 0.09);
      padding: 2.8rem 2.3rem 2.3rem 2.3rem;
      min-height: 66dvh;
      backdrop-filter: var(--blur);
      -webkit-backdrop-filter: var(--blur);
      border: 1.4px solid #d4dfcfcc;
      transition: box-shadow 0.28s ease, border-color 0.28s ease;
      will-change: box-shadow;
      user-select: text;
      overflow-wrap: break-word;
      word-break: break-word;
    }
    main.container:focus-within,
    main.container:hover {
      box-shadow: 0 18px 66px 0 #b0b99b33, 0 6px 22px 0 #a0bb8c2e;
      border-color: var(--accent);
    }
    /* --- Footer --- */
    .footer {
      text-align: center;
      font-size: 0.99rem;
      color: #7d8e7a;
      margin-bottom: 0.9rem;
      margin-top: 1.9rem;
      letter-spacing: 0.01em;
      background: none;
      opacity: 0.91;
      user-select: none;
      -webkit-user-select: none;
    }
    /* --- Scrollbar Styling --- */
    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
      background: transparent;
    }
    ::-webkit-scrollbar-thumb {
      background-color: rgba(181 198 164 / 0.18);
      border-radius: 8px;
    }
    /* --- Responsive Mobile --- */
    @media (max-width: 680px) {
      .main-nav {
        padding: 0.58rem 0.38rem;
      }
      .main-nav .logo img {
        height: 42px;
        width: 42px;
      }
      .nav-menu {
        flex-direction: column;
        align-items: flex-end;
        gap: 0.08em;
        position: fixed;
        top: 64px;
        right: 12px;
        background: var(--nav-bg);
        padding: 1.3em 1em 1em 1em;
        border-radius: 1.5rem;
        box-shadow: none;
        min-width: 175px;
        display: none;
        user-select: none;
        backdrop-filter: var(--blur);
        -webkit-backdrop-filter: var(--blur);
      }
      .nav-menu.open {
        display: flex;
        animation: navPop 0.2s ease forwards;
      }
      .hamburger-btn {
        display: flex;
      }
      main.container {
        margin: 1.3rem 0.15rem 2rem 0.15rem;
        padding: 1.2rem 1rem;
        min-height: 58dvh;
        box-shadow: 0 10px 26px rgba(120 137 116 / 0.18);
        border-radius: var(--radius);
        border: 1.2px solid #c8d0bdcc;
      }
      .footer {
        font-size: 0.85rem;
        margin: 1rem 0 1.5rem 0;
        color: #6d7a64;
      }
    }
    /* --- Verhindert horizontales Scrollen bei breiten Inhalten --- */
    img, table, video, iframe, input, textarea, select {
      max-width: 100%;
      box-sizing: border-box;
      word-wrap: break-word;
      overflow-wrap: break-word;
    }
  </style>
</head>
<body>
  <nav class="main-nav" role="navigation" aria-label="Hauptnavigation">
    <a href="/" class="logo" tabindex="0" aria-label="Spießbuch Startseite">
      <img src="/logo.png" alt="Logo Spießbuch" />
      Spießbuch
    </a>
    <% if (user) { %>
      <button class="hamburger-btn" id="hamburger-btn" aria-label="Menü öffnen" aria-expanded="false" aria-controls="main-menu" tabindex="0">
        <span></span><span></span><span></span>
      </button>
      <ul class="nav-menu" id="main-menu" role="menu" tabindex="-1">
        <li role="none"><a role="menuitem" href="/dashboard" tabindex="-1">Dashboard</a></li>
        <% if (user.is_admin) { %>
          <li role="none"><a role="menuitem" href="/penalties" tabindex="-1">Strafen</a></li>
          <li role="none"><a role="menuitem" href="/users" tabindex="-1">Nutzer</a></li>
        <% } %>
        <li role="none"><a role="menuitem" href="/profil" tabindex="-1">Profil</a></li>
        <li role="none"><a role="menuitem" href="/ranking" tabindex="-1">Ranking</a></li>
        <li role="none"><a role="menuitem" href="/export" tabindex="-1">Export</a></li>
        <li role="none"><a role="menuitem" href="/strafenkatalog.pdf" target="_blank" rel="noopener" tabindex="-1">📄 Strafenkatalog</a></li>
        <li role="none">
          <form action="/logout" method="POST" style="display:inline; margin:0;">
            <button type="submit" class="btn btn-logout" title="Logout" tabindex="-1" aria-label="Logout">
              <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round"><path d="M9 16l-1.5-1.5M9 16l1.5-1.5M9 16h7"/><path d="M16 12v-6a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v6"/><polyline points="19 16 22 12 19 8"/><line x1="22" y1="12" x2="9" y2="12"/></svg>
            </button>
          </form>
        </li>
      </ul>
    <% } %>
  </nav>

  <% if (typeof noContainer !== 'undefined' && noContainer) { %>
    <main><%- body %></main>
  <% } else { %>
    <main class="container" tabindex="0"><%- body %></main>
  <% } %>

  <footer class="footer" role="contentinfo" tabindex="-1">
    &copy; <%= new Date().getFullYear() %> GutSchlag Spießbuch v.1.0 by RS
  </footer>

  <script>
    const btn = document.getElementById('hamburger-btn');
    const menu = document.getElementById('main-menu');
    if(btn && menu){
      btn.onclick = () => {
        menu.classList.toggle('open');
        btn.classList.toggle('open');
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!expanded));
      };
      window.addEventListener('resize',()=> {
        menu.classList.remove('open');
        btn.classList.remove('open');
        btn.setAttribute('aria-expanded', 'false');
      });
      document.body.addEventListener('click',e=>{
        if(menu.classList.contains('open') && !menu.contains(e.target) && !btn.contains(e.target)) {
          menu.classList.remove('open');
          btn.classList.remove('open');
          btn.setAttribute('aria-expanded', 'false');
        }
      },true);
    }
  </script>
</body>
</html>
