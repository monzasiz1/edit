<h2 class="ranking-title">Ranking der Benutzer</h2>
<table class="ranking-table">
  <thead>
    <tr>
      <th>Platz</th>
      <th>Benutzername</th>
      <th>Strafen</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach((user, index) => { %>
      <% if (user.id === userId) { %> <!-- Der aktuelle Benutzer wird hervorgehoben -->
        <tr class="highlight-user">
          <td><%= index + 1 %></td>
          <td><%= user.username %></td>
          <td><%= user.penalty_count %></td> <!-- Verwende die gezählte Anzahl der Strafen -->
        </tr>
      <% } else { %> <!-- Alle anderen Benutzer werden abgeblendet -->
        <tr class="blurred">
          <td><%= index + 1 %></td>
          <td><%= user.username %></td>
          <td><%= user.penalty_count %></td> <!-- Verwende die gezählte Anzahl der Strafen -->
        </tr>
      <% } %>
    <% }); %>
  </tbody>
</table>
