<section class="export-container">
  <h1>📤 Exportfunktionen</h1>

  <div class="export-section">
    <h2>⬇️ Meine Strafen exportieren</h2>
    <p class="export-desc">Exportiere deine persönlichen Strafen als PDF oder CSV.</p>
    <div class="btn-group">
      <a href="/export/meine-pdf" class="btn btn-export">PDF herunterladen</a>
      <a href="/export/meine-csv" class="btn btn-export-secondary">CSV herunterladen</a>
    </div>
  </div>

  <% if (user && user.is_admin) { %>
  <div class="export-section admin-section">
    <h2>👑 Alle Strafen exportieren (Admin)</h2>
    <p class="export-desc">Exportiere alle Strafen aller Mitglieder als PDF oder CSV.</p>
    <div class="btn-group">
      <a href="/export/alle-pdf" class="btn btn-export">PDF (Alle Mitglieder)</a>
      <a href="/export/alle-csv" class="btn btn-export-secondary">CSV (Alle Mitglieder)</a>
    </div>

    <h3>👤 Einzelne Mitglieder exportieren</h3>
    <p class="export-desc">Exportiere die Strafen eines bestimmten Mitglieds.</p>
    <form action="/users" method="get" style="margin-top: 0.3em;">
      <button type="submit" class="btn btn-user-admin">Zur Nutzerverwaltung</button>
    </form>
  </div>
  <% } %>
</section>

<style>
.export-container {
  max-width: 500px;
  margin: 2.2rem auto 2.5rem auto;
  padding: 2.5rem 1.6rem 2.5rem 1.6rem;
  background: #fff;
  border-radius: 22px;
  box-shadow: 0 8px 40px 0 #3048242d, 0 2px 10px 0 #0001;
  display: flex;
  flex-direction: column;
  gap: 2.2rem;
}

.export-container h1 {
  font-size: 2.2rem;
  color: #365634;
  font-weight: 800;
  text-align: center;
  margin-bottom: 1.5rem;
  letter-spacing: -1px;
}

.export-section {
  background: #f6faf7;
  border-radius: 16px;
  padding: 1.5rem 1.3rem 1.4rem 1.3rem;
  box-shadow: 0 2px 16px #a2c0b83b;
  margin-bottom: 1.4rem;
  border: 1.5px solid #ecf1ea;
}

.export-section h2 {
  font-size: 1.2rem;
  color: #486c48;
  font-weight: 700;
  margin-bottom: 0.7rem;
  display: flex;
  align-items: center;
  gap: 0.5em;
}

.export-section h3 {
  margin-top: 1.2em;
  color: #657b5a;
  font-size: 1.07em;
  font-weight: 700;
  letter-spacing: 0;
}

.export-desc {
  color: #636e5a;
  font-size: 0.97em;
  margin-bottom: 1.1em;
}

.btn-group {
  display: flex;
  gap: 1.2em;
  margin-bottom: 0.6em;
  flex-wrap: wrap;
}

.btn.btn-export,
.btn.btn-user-admin {
  background: linear-gradient(90deg, #446652 70%, #658d63 100%);
  color: #fff !important;
  font-weight: 700;
  font-size: 1.03em;
  border: none;
  border-radius: 2em;
  padding: 0.68em 1.35em;
  box-shadow: 0 2px 8px #60784822;
  text-decoration: none;
  transition: background 0.16s, box-shadow 0.16s;
}
.btn.btn-export:hover,
.btn.btn-user-admin:hover {
  background: linear-gradient(90deg, #364e35 70%, #82b081 100%);
  box-shadow: 0 4px 24px #36563422;
}

.btn.btn-export-secondary {
  background: #f4f8f5;
  color: #446652 !important;
  font-weight: 700;
  border: 1.2px solid #b4cbb4;
  border-radius: 2em;
  padding: 0.68em 1.35em;
  box-shadow: 0 2px 8px #c5d0bd18;
  text-decoration: none;
  transition: border 0.15s, box-shadow 0.16s;
}
.btn.btn-export-secondary:hover {
  background: #d3e4d9;
  color: #314b30 !important;
  border-color: #607848;
  box-shadow: 0 4px 24px #486c4822;
}

.btn.btn-user-admin {
  background: #607848;
  color: #fff !important;
  border: none;
  margin-top: 0.1em;
  font-size: 1.02em;
  padding: 0.61em 1.08em;
}

.admin-section {
  background: #f6f2ff;
  border: 1.5px solid #e6dbfd;
  box-shadow: 0 2px 16px #7e5dea15;
}
.admin-section h2 { color: #7d4bba; }
.admin-section .btn.btn-export { background: linear-gradient(90deg, #6d3be8 60%, #a66be6 100%); }
.admin-section .btn.btn-export:hover { background: linear-gradient(90deg, #6040be 60%, #bb9add 100%); }

@media (max-width: 600px) {
  .export-container { padding: 1.2rem 0.3rem 2rem 0.3rem; }
  .export-container h1 { font-size: 1.33rem; margin-bottom: 0.9rem; }
  .export-section, .admin-section { padding: 1rem 0.4rem 1.1rem 0.4rem; }
  .btn-group { gap: 0.5em; }
  .btn.btn-export, .btn.btn-export-secondary, .btn.btn-user-admin { font-size: 0.97em; padding: 0.6em 0.95em; }
}
</style>
